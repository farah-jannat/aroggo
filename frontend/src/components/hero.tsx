import Carousel from "@/components/carousel";
import React from "react";

const Hero = () => {
  return (
    <div className=" px-[16px] py-[16px]">
      <Carousel />
    </div>
  );
};

export default Hero;
